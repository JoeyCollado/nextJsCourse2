import React from 'react'

export const metadata = { //defining own metadata in a route
    title: "About Page",
    description: "This is about page metadata"
}

const page = () => {
  return (
    <div>
      About
    </div>
  )
}

export default page
