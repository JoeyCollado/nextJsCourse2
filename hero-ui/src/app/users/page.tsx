import React from 'react'

export const metadata = { //defining own metadata in a route
    title: "Users Page",
    description: "This is users page metadata"
}

const page = () => {
  return (
    <div>
      Users
    </div>
  )
}

export default page
