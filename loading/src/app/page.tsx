import React from 'react'

const page = () => {
  return (
    <div>
      Home
    </div>
  )
}

export default page

//loading.tsx = template ui that is shown when app is waiting for component to load or data to fetch
