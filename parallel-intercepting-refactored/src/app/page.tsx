import React from 'react'

const page = () => {
  return (
    <div>
      
    </div>
  )
}

export default page

//parallel interecepting real power = instead of rendering entire page ui, you can just render a pop up model
// parallel route + intercepting route
//if you want to use parallel route with intercepting route
//first define parallel route
//seondly define intercepting route inside parallel route
//thirdly define the main route inside the intercepting route
//lastly define default.tsx inside intercepting route
//lastly define layout.tsx and just register your route inside it