import React from 'react'

const page = () => {
  return (
    <div>
      
    </div>
  )
}

export default page

//to create a parallel route = simply prefix the folder name with @
//parallel routes = are special components that allows us to use the loading.tsx and also error.tsx template and many more