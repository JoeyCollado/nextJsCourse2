import React from 'react'

const page = () => {
  return (
    <div>
      
    </div>
  )
}

export default page


//private pages
//to make a page privae just add _ as a prefix to whatever the folder's name


//after putting parenthesis in the folder name we can directly access the sub routes register and login withouth auth prefix