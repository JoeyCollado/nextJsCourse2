import React from 'react'

const page = () => {
  return (
    <div>
      Home
    </div>
  )
}

export default page

//template = like layouts because they're used in multiple pages, when a user moves to different pages, that use the same template, everything resets


// just like layout but it refreshes